services:
  ticket-monitor:
    build: .
    container_name: ticket-monitor
    restart: unless-stopped
    volumes:
      - ./data:/app/data:Z
    environment:
      STATE_FILE_PATH: /app/data/state.json
      LOG_FILE_PATH: /app/data/monitor.log
    env_file:
      - .env
